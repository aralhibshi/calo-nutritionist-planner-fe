import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#36B775',
      contrastText: '#FFFFFF'
    },
    secondary: {
      main: '#33FF57'
    }
  },
});

export default theme;